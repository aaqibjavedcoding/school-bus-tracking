# React Native 0.81 (Expo SDK 54) peers on react 19.1.0 while the web app
# pins react 18.3.1. The lockfile already carries that combination, so npm can
# only re-resolve the tree — e.g. when adding native mobile dependencies —
# with peer conflicts ignored. This is the standard setup for Expo monorepos.
legacy-peer-deps=true
