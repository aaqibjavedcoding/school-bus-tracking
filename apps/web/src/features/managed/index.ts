'use client';

export { ManagedSchoolProvider, useManagedSchool } from './ManagedSchoolProvider';
export { ManagedSchoolBanner } from './ManagedSchoolBanner';
export {
  clearManagedSchool,
  getManagedSchool,
  readManagedSchoolId,
  setManagedSchool,
  subscribeManagedSchool,
  type ManagedSchoolState,
} from './managed-school-store';
