/**
 * Background Workers & Queue Processors (Phase 1 placeholder)
 * Telemetry ingestion workers, notification dispatchers, and scheduled jobs will be registered here in Phase 2.
 */

export const workers = [];
