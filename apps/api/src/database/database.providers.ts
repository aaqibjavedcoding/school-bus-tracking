import { Provider } from '@nestjs/common';

/**
 * Custom database providers (Phase 1 placeholder)
 * Custom Sequelize providers, hooks, and connection factories can be declared here.
 */
export const databaseProviders: Provider[] = [];
